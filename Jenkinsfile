pipeline {
    stages {
        stage('build') {
            steps {
                sh 'php --version'
		sh 'echo output'
            }
        }
	stage('test') {
	    steps {
                bash 'echo running tests'
            }
	}
    }
}
